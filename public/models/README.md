# Model file required

This app expects the MediaPipe FaceLandmarker model at:

- `public/models/face_landmarker.task`

To get the file, download the official **FaceLandmarker** `.task` model from MediaPipe and place it at that path.

If you don't provide it, the app will show a model-load error.

